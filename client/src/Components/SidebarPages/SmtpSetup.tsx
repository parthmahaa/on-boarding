import React from 'react';

const SmtpSetup = () => (
  <div>
    <h1 className="text-2xl font-semibold mb-4">SMTP Setup</h1>
    <p className="text-gray-500">Configure your SMTP settings here.</p>
  </div>
);

export default SmtpSetup;
